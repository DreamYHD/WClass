<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.wclass">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@drawable/timg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".classFuture.inner.ClassActivity" />
        <activity android:name=".adoutFuture.RegistActivity" />
        <activity android:name=".adoutFuture.LoginActivity" />
        <activity android:name=".classFuture.CreateClassActivity" />
        <activity android:name=".classFuture.JoinClassActivity" />
        <activity android:name=".classFuture.inner.doSome.DoCreateQuestionActivity" />
        <activity android:name=".classFuture.inner.doSome.DoShowQuestionActivity" />
        <activity android:name=".classFuture.inner.doSome.SignedActivity"></activity>
    </application>

</manifest>